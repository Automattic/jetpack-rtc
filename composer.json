{
	"name": "automattic/jetpack-rtc",
	"description": "Real-time collaboration websocket transport support using WordPress.com infrastructure",
	"type": "jetpack-library",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=7.4",
		"automattic/jetpack-assets": "^5.0.2",
		"automattic/jetpack-connection": "^9.1.2-alpha"
	},
	"require-dev": {
		"automattic/phpunit-select-config": "^1.0.11",
		"yoast/phpunit-polyfills": "^4.0.0"
	},
	"autoload": {
		"classmap": [
			"src/"
		]
	},
	"scripts": {
		"build-development": [
			"pnpm run build"
		],
		"build-production": [
			"NODE_ENV=production BABEL_ENV=production pnpm run build"
		],
		"phpunit": [
			"phpunit-select-config phpunit.#.xml.dist --colors=always"
		],
		"test-php": "@composer phpunit",
		"test-php-coverage": "php -dpcov.directory=. ./vendor/bin/phpunit-select-config phpunit.#.xml.dist --coverage-php \"$COVERAGE_DIR/php.cov\""
	},
	"minimum-stability": "dev",
	"prefer-stable": true,
	"suggest": {
		"automattic/jetpack-autoloader": "Allow for better interoperability with other plugins that use this package."
	},
	"extra": {
		"autotagger": true,
		"branch-alias": {
			"dev-trunk": "0.2.x-dev"
		},
		"changelogger": {
			"link-template": "https://github.com/Automattic/jetpack-rtc/compare/v${old}...v${new}"
		},
		"mirror-repo": "Automattic/jetpack-rtc",
		"textdomain": "jetpack-rtc",
		"version-constants": {
			"::PACKAGE_VERSION": "src/class-rtc.php"
		}
	}
}
